#include "philo.h"

int check_param(int ac, char **av)
{
    int i = 0;

    while(i < ac)
    {
        
    }
}

int main(int ac, char **av)
{
    t_input input;
    if(!(ac == 5 || ac == 6))
        return 1;
    if(check_param(ac,av))
}